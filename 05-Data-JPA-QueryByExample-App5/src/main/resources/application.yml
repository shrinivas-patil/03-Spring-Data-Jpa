spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sbms27
    username: root
    password: Nivas@2001
    driver-class-name: com.mysql.cj.jdbc.Driver 

  jpa:
    hibernate:
      ddl-auto: update #if table is not availabe in sql table should dynamically created in database

    show-sql: true #it is used for to print the queries on consule